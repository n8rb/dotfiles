" When I use the tab button, insert spaces
set expandtab
" Existing tab characters should be two spaces long
set tabstop=2
" Indentation via other means should also be two spaces long
set shiftwidth=2

" Normal line wrapping
set linebreak
